BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[SurveyAttempt] ADD [currentPage] INT NOT NULL CONSTRAINT [SurveyAttempt_currentPage_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
