BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[SurveyAttempt] ADD [aiFeedbackJson] NVARCHAR(max);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
