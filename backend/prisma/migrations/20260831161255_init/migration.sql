BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[TestConnection] (
    [id] INT NOT NULL IDENTITY(1,1),
    [message] NVARCHAR(1000) NOT NULL,
    [createdAt] DATETIME2 NOT NULL CONSTRAINT [TestConnection_createdAt_df] DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT [TestConnection_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
